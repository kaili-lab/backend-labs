# stage-00：最小可用的单进程 Web Server

## 当前问题
这是起点阶段，目标不是解决高并发，而是先建立一个最小可运行的请求处理模型。

## 这一阶段要做什么
- 提供最基本的 HTTP 请求处理能力
- 明确请求进入、处理、返回响应的基本流程
- 为后续阶段提供对照基线

## 这一阶段先不解决什么
- 不追求高吞吐
- 不引入线程池、缓存、队列等机制
- 不提前解决未来还没出现的瓶颈
